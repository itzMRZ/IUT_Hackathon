# ── Office server (server/index.ts) ─────────────────────────────────────
PORT=3001

# ── Dashboard (src/) ─────────────────────────────────────────────────────
# Not needed for a single-VPS deployment — Nginx proxies /api and /ws to
# the Node server on the same host, so relative paths just work. Only set
# these if the dashboard and server are deployed on *different* hosts.
# VITE_API_URL=https://your-server-host
# VITE_WS_URL=wss://your-server-host/ws

# ── Discord webhook (posts on every simulation change) ──────────────────
# Server Settings → Integrations → Webhooks → New Webhook → copy URL
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# ── Discord bot (discord/bot.ts) ─────────────────────────────────────────
# Discord Developer Portal → New Application → Bot → Reset Token
DISCORD_TOKEN=your-bot-token
DISCORD_PREFIX=!
# Channel ID to post live alerts to (right-click channel → Copy Channel ID,
# requires Developer Mode enabled in Discord settings)
DISCORD_ALERT_CHANNEL_ID=

# Where the bot reaches the office server's REST + WebSocket API.
# On a single VPS this is just localhost since both run on the same host.
API_URL=http://localhost:3001
# WS_URL=ws://localhost:3001/ws
