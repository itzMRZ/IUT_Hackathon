# Office server
PORT=3001

# Dashboard WebSocket (optional — Vite proxy handles dev)
# VITE_WS_URL=ws://localhost:3001/ws

# Discord webhook — posts on every simulation change (toggle, preset, auto-sim tick, alerts)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN
